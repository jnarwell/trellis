/**
 * Test Utilities for @trellis/client
 *
 * Provides mock implementations and test helpers for
 * Storybook stories and unit tests.
 */

export { mockClient, mockEntities } from './mock-client.js';
export type { MockClient, MockEntity, QueryBuilder } from './mock-client.js';
